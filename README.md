# forward-bot-telegram

Telegram bot that mirrors a source channel to multiple targets (aiogram 3).

Setup: copy `.env.example` to `.env` and set `BOT_TOKEN`. Optional `SOURCE_CHANNEL_ID`; source can also be set with `/set_source`.

See `requirements.txt` — run with `python bot.py`.
